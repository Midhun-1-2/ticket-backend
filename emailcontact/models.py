# Intentionally empty — see authentication/models.py for the actual
# CompanyContactSettingsProxy model, which declares app_label = "emailcontact"
# so it's grouped under this app's section in the Django admin without moving
# the real table out of `authentication`.

# Intentionally empty — see authentication/models.py for the actual
# LoginActivityProxy / StaffActivityProxy models, which declare
# app_label = "activitylog" so they're grouped under this app's section
# in the Django admin without moving the real tables out of `authentication`.
